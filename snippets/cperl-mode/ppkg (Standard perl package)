# -*- mode: snippet -*-
# name: ppkg (Standard perl package)
# key: ppackage
# expand-env: ((package-name (sbw/default-perl-package-name)))
# --
package `package-name`;

use 5.014;
use warnings;

use Carp;
use Scalar::Util qw/looks_like_number blessed/;

$0

1;
